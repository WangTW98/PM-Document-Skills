interface:
  display_name: "Product Page Release"
  short_description: "Revise or finalize one page draft"
  default_prompt: "Use $product-page-release to read exactly one latest product/development/pages/*.md page draft, including versioned drafts when present. Unless I explicitly ask for the final/release/正式版 output, substantively regenerate the next versioned draft by applying completed PA-/PQ- decisions plus any 用户补充描述 modifications into the page body, and keep an empty 用户补充描述 plus remaining/new confirmation list. Do not create a version-only copy. Only on explicit final release, write one confirmed page spec under product/release/pages with no assumptions, open questions, or raw supplement notes."
