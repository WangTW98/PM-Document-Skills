interface:
  display_name: "Product Layout Release"
  short_description: "Confirm project layout contract"
  default_prompt: "Use $product-layout-release to read the latest product/development/layout/product-layout-draft*.md, apply LA-/LQ- Release handling decisions plus any 用户补充描述 modifications, and create product/release/layout/product-layout-release.md as the confirmed layout dependency with no assumptions, open questions, confirmation workflow sections, or raw supplement notes. For iterative draft updates, use $product-layout-draft-update instead."
