interface:
  display_name: "Product Layout Release"
  short_description: "Revise or finalize layout drafts"
  default_prompt: "Use $product-layout-release to read the latest product/development/layout/product-layout-draft*.md draft. Unless I explicitly ask for the final/release/正式版 output, substantively regenerate the next versioned draft by applying completed LA-/LQ- decisions plus any 用户补充描述 modifications into the layout body, and keep an empty 用户补充描述 plus remaining/new confirmation list. Do not create a version-only copy. Only on explicit final release, create product/release/layout/product-layout-release.md as the confirmed layout dependency with no raw supplement notes."
