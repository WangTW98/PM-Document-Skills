interface:
  display_name: "Product Layout Draft Update"
  short_description: "Revise layout draft with edits"
  default_prompt: "Use $product-layout-draft-update to read the latest product/development/layout/product-layout-draft*.md and the layout draft template, merge all user-confirmed LA-/LQ- decisions and 用户补充描述 into the layout body, preserve every required template chapter/table in order, add new layout assumptions/questions from the updated result, reset 用户补充描述 to an empty placeholder, and save the next product-layout-draft-v<N>.md. Do not create release output."
